venv/
__pycache__/
*.pyc
.env
*.db
*.sqlite3

node_modules/
dist/
.vercel/

.DS_Store
Thumbs.db

# Database snapshots — contain password/recovery hashes, never commit
backend/backups/

# Session signing key — never commit; committing it lets anyone forge a login
backend/.secret_key
